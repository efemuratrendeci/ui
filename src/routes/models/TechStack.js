export default class TechStack {
    constructor(name, rate, src, additionalClassName) {
        this.name = name;
        this.rate = rate;
        this.src = src;
        this.additionalClassName = additionalClassName;
    }
}